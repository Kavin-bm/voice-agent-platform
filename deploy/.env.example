JWT_SECRET=change-me
PLATFORM_ADMIN_API_KEY=change-me

# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
CREDENTIAL_ENCRYPTION_KEY=change-me

DOGRAH_WEBHOOK_SECRET=change-me
INTERNAL_TOOL_SECRET=change-me

# From the api/worker containers' point of view, not the host's — see
# "Running Dograh locally" in the root README for why these two differ.
DOGRAH_BASE_URL=http://host.docker.internal:18080
DOGRAH_CALLBACK_BASE_URL=http://host.docker.internal:8000

DASHBOARD_ORIGINS=http://localhost:3000,http://localhost:3001
